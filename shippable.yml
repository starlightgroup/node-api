language: node_js
node_js:
  - '6'
services:
  - redis
env:
  - NODE_ENV=production
build:
  ci:
    - npm install
    - npm install -g babel-cli 
    - npm install -g snyk
    - npm run postinstall
    - npm start
  
